import { Path, G, Defs, Use } from "react-native-svg"

const handsTypeA = {
  // COLOR SCHEME 0
  hands_A_0: {
    obj: (colors) => (
      <></>
    ),
    M: true,
    F: true,
    scheme: 0,
    type: "A"
  },
}

export default handsTypeA;
